<div class="pb-4 mb-4 border-b">
  <h2 class="text-xl font-semibold"><%= post.title %></h2>
  <p class="text-gray-700"><%= truncate(post.content, length: 150) %></p>
  <p class="text-sm text-gray-500">Posted by <%= post.user.email %></p>
</div>